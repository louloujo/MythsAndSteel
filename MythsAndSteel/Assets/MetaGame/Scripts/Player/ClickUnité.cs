using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClickUnité : MonoBehaviour
{
    /*
    void ClicSurUnit(UnitScript uniTouch)
    {
        if (uniTouch.IsActivationDone == false)
        {
            if (GameManager.Instance.IsPlayer1Starting && GameManager.Instance.ActualTurnPhase == MYthsAndSteel_Enum.PhaseDeJeu.ActionJ1)
            {
                PlayerStatic.CheckIsUnitArmy(uniTouch, true);
                foreach (MYthsAndSteel_Enum.TypeUnite type in PlayerScript.Instance.DisactivateUnitType)
                {
                    if (uniTouch.UnitSO.typeUnite != type)
                    {
                        PlayerScript.Instance.actualUnitClic == uniTouch;
                    }
                }
            }

            if (!GameManager.Instance.IsPlayer1Starting && GameManager.Instance.ActualTurnPhase == MYthsAndSteel_Enum.PhaseDeJeu.ActionJ1)
            {
                PlayerStatic.CheckIsUnitArmy(uniTouch, false);
                foreach (MYthsAndSteel_Enum.TypeUnite type in PlayerScript.Instance.DisactivateUnitType)
                {
                    if (uniTouch.UnitSO.typeUnite != type)
                    {
                        PlayerScript.Instance.actualUnitClic == uniTouch;
                    }
                }
            }

            if (GameManager.Instance.IsPlayer1Starting && GameManager.Instance.ActualTurnPhase == MYthsAndSteel_Enum.PhaseDeJeu.ActionJ2)
            {
                PlayerStatic.CheckIsUnitArmy(uniTouch, false);
                foreach (MYthsAndSteel_Enum.TypeUnite type in PlayerScript.Instance.DisactivateUnitType)
                {
                    if (uniTouch.UnitSO.typeUnite != type)
                    {
                        PlayerScript.Instance.actualUnitClic == uniTouch;
                    }
                }
            }

            if (!GameManager.Instance.IsPlayer1Starting && GameManager.Instance.ActualTurnPhase == MYthsAndSteel_Enum.PhaseDeJeu.ActionJ2)
            {
                PlayerStatic.CheckIsUnitArmy(uniTouch, true);
                foreach (MYthsAndSteel_Enum.TypeUnite type in PlayerScript.Instance.DisactivateUnitType)
                {
                    if (uniTouch.UnitSO.typeUnite != type)
                    {
                        PlayerScript.Instance.actualUnitClic == uniTouch;
                    }
                }
            }
        }
    }
    */
}
